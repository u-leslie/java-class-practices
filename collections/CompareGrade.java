package collections;

import java.util.Comparator;

public class CompareGrade implements Comparator {
    @Override
    public int compare(Student s1, Student s2){
        if(s1.grade>s2.grade)
    }
}
